import { StyleSheet } from "react-native";

const styled = StyleSheet.create({
  todoContainer: {
    padding: 15,
    flex: 1,
  },
  todo: {
    borderColor: "#000",
    borderWidth: 1,
    borderRadius: 10,
    padding: 20,
    marginBottom: 15,
  },
});

export default styled;
