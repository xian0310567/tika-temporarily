import axios from "axios";

import Auth from "@screens/Auth";

const useVerificateAuthData = () => {
  const VerificateAuthData = () => {
    const data = "";
    // axios

    if (!data) {
      console.log("Aa");
      return <Auth />;
    }
  };

  return { VerificateAuthData };
};

export default useVerificateAuthData;
